
#include<stdio.h>

 void main() {
 
	 int n;
	 printf("Enter the size of array : ");
	 scanf("%d",&n);
	 int z = 0, num2[n];

	 int num[n],num3[n];
	 printf("Enter the element in array : ");
	 
	 for(int i = 0; i < n; i++) {
	 
		 scanf("%d",&num[i]);
	 }
	 for(int i = 0; i < n; i++) {
	 
		 int num3[i] = num[]
		 int count = 1;
		 for(int j = i + 1; j < n; j++) {
		 
			 if(num[i] == num[j]) {
				 count++;
				 num2[z] = num[i];
				 z++;
			 }
	 


		 }
	 }
	 for(int i = 0; i < n; i++) {
	 

	 }
 }
